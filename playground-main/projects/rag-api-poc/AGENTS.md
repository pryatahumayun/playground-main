# AGENTS

- Keep the implementation focused on the proof-of-concept requirements.
- Preserve the separation between ingestion, query, and infrastructure clients.
- Keep AWS and OpenSearch integrations behind interfaces so mocks can be used locally.
- Do not hard-code credentials.
